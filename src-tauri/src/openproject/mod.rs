//! The OpenProject API layer: URL assembly, filter encoding, attachment URL
//! rewriting, and the one HTTP client that uses them.

pub mod attachment_urls;
pub mod client;
pub mod filters;
pub mod url;
